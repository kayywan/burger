var connection = require("../config/connection.js");

function selectAll();

function insertOne()
{
    var arr = [];
    
    for (var i = 0; i < Number; i++) {
        arr.push("?");
    }
  
    return arr.toString();
};
  

function updateOne();

module.exports = orm;
